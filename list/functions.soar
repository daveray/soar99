##!
# Common "functions"

sp {list*identity*apply
  "Identity operator"
  (state <s> ^operator <o>)
  (<o> ^name list*identity
       ^value <value>)
-->
  (<s> ^result <value>)
}
       
