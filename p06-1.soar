source "p06.soar"
# failure case
sp {apply*init
  (state <s> ^operator.name init)
-->
  (<s> ^name p06
       ^input (list this is not a palindrome a is this)
       ^expected-result false)
}

