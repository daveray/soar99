source "test-list-map.soar"

sp {test-list-map*apply*init
  (state <s> ^operator.name init)
-->
  (<s> ^name test-list-map
       ^input nil)
}

sp {succeeded
  (state <s> ^name test-list-map
             ^result nil)
-->
  (write (crlf) |Succeeded|)
  (halt)
}

