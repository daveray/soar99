source ../common.soar

sp {apply*double
  "Multiply a value by 2"
  (state <s> ^operator <o>)
  (<o> ^name double
       ^value <value>
       ^index <index>)
-->
  (<s> ^result (* <value> 2))
  (write (crlf) |Applying double to | <value> | at index | <index>)
}

sp {test-list-map*propose*map
  "Map 'double' operator on the input list"
  (state <s> ^name test-list-map
            -^result
             ^input <input>)
-->
  (<s> ^operator <o>)
  (<o> ^name list*map
       ^func <f>
       ^head <input>)
  (<f> ^name list*multiply
       ^args.factor 2)
}

