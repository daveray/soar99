# Find the kth element of a list

test "P03: Find the kth element of a list" {
  sp {pass
    (state <s> ^superstate nil)
  -->
    (pass |See bebot*list*get for implementation of problem 3|)
  }
}
