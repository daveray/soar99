
sp {elaborate*operator*attributes
  (state <s> ^superstate.operator.<name> <value>)
-->
  (<s> ^<name> <value>)
}

source "list.soar"

sp {propose*init
  (state <s> ^superstate nil
            -^name)
-->
  (<s> ^operator.name init)
}

