
source "../bebot/bebot.soar"
#source http://github.com/daveray/bebot/raw/master/bebot.soar

sp {propose*init
  (state <s> ^superstate nil
            -^name)
-->
  (<s> ^operator.name init)
}

