# Find the number of elements in a list

test "P04: Find the number of elements in a list" {
  sp {pass
    (state <s> ^superstate nil)
  -->
    (pass |See bebot*list*size for implementation of problem 4|)
  }
}
